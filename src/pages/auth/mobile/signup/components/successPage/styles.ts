import styled from "styled-components";

import { PlainText } from "@pages/auth/mobile/components/text";

export const BannerText = styled(PlainText)`
    font-weight: 700;
    font-size: 30px;
    line-height: 47px;
`;

export const ConfettiLogo = styled.img`
    margin-top: 100px;
`;

export const WelcomeText = styled(PlainText)`
    font-weight: 700;
    font-size: 40px;
    padding: 20;
    margin-top: 37px;
`;
